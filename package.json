{
  "name": "gemini",
  "version": "1.0.0",
  "description": "A Cloudflare Worker proxy for the free2gpt.xyz API",
  "main": "src/index.js",
  "scripts": {
    "build": "npx wrangler deploy --minify --dry-run --outdir=./dist"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
